import nltk
nltk.download('punkt')  # Already done, but include it again to ensure it's complete
nltk.download('wordnet')  # Already done, but include it again
nltk.download('omw-1.4')  # Ensure the WordNet lemmatizer works correctly
nltk.download('tokenizers/punkt_tab')  # Install the punkt_tab tokenizer package
